services:
  - type: web
    name: wb-bot
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python3 wb_bot.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.11
